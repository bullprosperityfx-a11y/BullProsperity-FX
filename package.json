{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run test:syntax && npm run test:api && npm run test:pages",
    "test:syntax": "node --check login.js && node --check member-platform.js && node --check access-guard.js && node --check sw.js",
    "test:api": "node scripts/test-api.mjs",
    "test:pages": "node scripts/validate-pages.mjs"
  },
  "dependencies": {
    "@vercel/functions": "^3.6.2"
  }
}
